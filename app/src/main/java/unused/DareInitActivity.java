package unused;

/*
public class DareInitActivity extends AppCompatActivity {
    private static final int CAMERA_REQUEST = 1888;
    private String gameId;
    private int positionOnTheBoard;
    private boolean isItMyTurn;
    private boolean amIPlayer1;
    private boolean amIRequestedAdditionalAction;
    private boolean isRemotePlayerRequestedAdditionalAction;

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);

        Intent intent = getIntent();
        Bundle extras = intent.getExtras();

        if (extras != null) {
            gameId = extras.getString("gameId");
            positionOnTheBoard = extras.getInt("positionOnTheBoard");
            isItMyTurn = extras.getBoolean("isItMyTurn");
            amIPlayer1 = extras.getBoolean("amIPlayer1");
            amIRequestedAdditionalAction = extras.getBoolean("amIRequestedAdditionalAction");
            isRemotePlayerRequestedAdditionalAction = extras.getBoolean("isRemotePlayerRequestedAdditionalAction");
        }

        final DareDialogFragment dareDialogFragment = DareDialogFragment.newInstance(gameId, positionOnTheBoard, isItMyTurn, amIPlayer1, amIRequestedAdditionalAction, isRemotePlayerRequestedAdditionalAction);
        dareDialogFragment.show(getSupportFragmentManager(), "Dare Fragment");
    }


} */
